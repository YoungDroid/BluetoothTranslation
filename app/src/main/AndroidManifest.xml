<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.oom.translatecommunication"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.PROCESS_OUTGOING_CALLS"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <uses-permission android:name="android.permission.RECEIVE_SMS"/>
    <uses-permission android:name="android.permission.READ_SMS"/>
    <uses-permission android:name="android.permission.SEND_SMS"/>
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:name=".app.ApplicationTranslateCommunication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.activity.ActivityMain_">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".view.activity.ActivityClient_">
        </activity>
        <activity
            android:name=".view.activity.ActivityService_">
        </activity>
        <activity
            android:name=".view.activity.ActivityTargetNumber_">
        </activity>

        <receiver android:name=".app.receiver.CcSmsReceiver">
            <intent-filter android:priority="999">
                <action android:name="candroid.intent.action.BOOT_COMPLETEN"/>
                <action android:name="android.provider.Telephony.SMS_RECEIVED"/>
            </intent-filter>
        </receiver>
        <receiver android:name=".app.receiver.CcPhoneReceiver">
            <intent-filter android:priority="1000">
                <action android:name="candroid.intent.action.BOOT_COMPLETEN"/>
                <action android:name="android.intent.action.NEW_OUTGOING_CALL"/>
                <action android:name="android.intent.action.PHONE_STATE"/>
            </intent-filter>
        </receiver>
    </application>
</manifest>